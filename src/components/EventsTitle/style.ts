import styled from "styled-components";
import { Color } from "ui";

const Title = styled.h2`
  padding-bottom: 2rem;
  font-size: 1.5rem;
  font-weight: 500;
  color: ${Color.TITLE};
`;
export { Title };
