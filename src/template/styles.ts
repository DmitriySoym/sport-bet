import styled from "styled-components";

const Template = styled.div`
  display: grid;
  grid-template-rows: auto 1fr auto;
  min-height: 100%;
  max-width: 900px;
  width: 100%;
  margin: 0 auto;
`;

export { Template };
